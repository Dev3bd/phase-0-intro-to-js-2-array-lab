// index.js

// Destructively removes the first cat from the cats array and returns a new array without the first cat.
function destructivelyRemoveFirstCat(cats) {
    // Use array method to remove the first cat
    // Return the modified array
  }
  
  // Nondestructively removes the first cat from the cats array and returns a new array without the first cat.
  function removeFirstCat(cats) {
    // Use array method to create a new array without modifying the original array
    // Return the new array
  }
  
  // Write other functions as per the requirements provided in the test cases
  
